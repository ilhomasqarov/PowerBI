2. Use the “Remove Duplicates” option from the Home tab or right-click the row.

3. It allows you to filter the column values (e.g., by value, number, text, or date).

4. Right-click the column header > Rename > type “CustomerID”.

5. It applies all changes and loads the transformed data into Power BI.

6. Use the filter dropdown on Quantity > choose “is greater than or equal to 2”.

7. Select OrderDate column > go to Add Column tab > choose Date > Year, Month, Day.

8. Right-click the Product column > Replace Values > replace “Mouse” with “Computer Mouse”.

9. Click the dropdown on OrderDate > Sort Descending.

10. Use “Replace Values” or “Fill Down”, or filter/remove nulls, depending on the context.

12. Use “Group By” on CustID, and aggregate by sum on TotalSpent or calculated column.

13. Change the column type to “Date” to standardize formats automatically.

14. Use “Add Column > Conditional Column”: If Price > 100 then “High Value” else “Normal”.

15. Remove unnecessary columns early in the Applied Steps to reduce data load and improve performance.
