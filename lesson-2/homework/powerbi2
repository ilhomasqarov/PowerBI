1. Excel, SQL Server, Web APIs

2. Click “Get Data” and choose a data source

3. Click the “Refresh” button on the Home tab

4. CSV, Excel

5. A preview of tables/sheets and their data

6. Use “Transform Data,” select “Product” and “Price,” remove other columns, then load

7. Right-click the column > Change Type > Date

8. “Load” brings in data as-is; “Transform Data” opens Power Query for editing

9. Invalid credentials or lack of access

10. Go to Data source settings > select source > click “Change Source”

12. Use Data source settings > Change Source > point to new file

13. Change the column type manually or clean data in Power Query

14. Use a SQL query with a WHERE clause or set up a parameter in Power Query

15. Use Power Automate to trigger a dataset refresh when data changes
